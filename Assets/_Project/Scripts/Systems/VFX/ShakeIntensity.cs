namespace _Project.Scripts.Systems.VFX
{
    public enum ShakeIntensity
    {
        Light,      // Hafif (single match)
        Medium,     // Orta (combo)
        Heavy,      // Ağır (big combo)
        Explosion,  // Patlama (powerup)
        Custom,     // Özel parametreler
    }
}